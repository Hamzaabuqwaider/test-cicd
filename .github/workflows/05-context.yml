name: 05-context
run-name: Hamza Qwaider explaining context data
on:
  workflow_dispatch

jobs:
  print-context-data:
    runs-on: ubuntu-latest
    steps:
      - name: Print Context Data
        run: echo ${{ toJSON(github) }}
